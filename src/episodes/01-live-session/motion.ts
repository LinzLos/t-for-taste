// The one settle curve this episode uses, so it is retuned in one place. composer.css carries
// the same numbers once, as cubic-bezier(.33,1,.68,1), for the pure-CSS entrance.
export const EASE: [number, number, number, number] = [0.33, 1, 0.68, 1]
